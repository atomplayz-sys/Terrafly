# TerraFly screenshot annotations

The original PNGs are unmodified live captures from the audited application. `build_ownership_pack.py` creates editable SVG overlays and flattened annotated PNGs.

## 01 live start screen

1. DEM Terrain is the recommended measured-elevation workflow.
2. Photo AI is visibly experimental and relative until evidence passes.
3. Source and datum fields make provenance part of the input, not an afterthought.

## 02 live DEM Terrain result

1. Scientific state reads `Metric Source DEM`.
2. The main surface dominates the result workspace.
3. Photo/Height colors, sun, wireframe, structures and navigation are inspection controls.
4. Numeric legend uses metres because values come from the source DEM.
5. Evidence downloads and warnings remain visible below the viewer.

## 03 live Photo AI calibrated

1. State reads `Metric Calibrated` only after the reference gate passed.
2. Geometry remains an inspection mesh while the metric analysis grid powers values.
3. Model revision/device and provenance distinguish real inference from test-only behavior.
4. Photo and height-color modes are separate.

## 04 live calibration passed

1. Passed badge reflects executable gates, not a manual UI switch.
2. Calibration report remains downloadable evidence.
3. The bundled pass is a synthetic software oracle, not a real model benchmark.

